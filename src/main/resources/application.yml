server:
  servlet:
    application-display-name: SAKPaaS
spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: zuschwer
app:
  presence:
    duration: 20
  version: 1.3.4
management:
  server:
    port: 9000
  endpoints:
    web:
      exposure:
        include: "*"
