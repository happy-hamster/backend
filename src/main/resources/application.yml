server:
  servlet:
    context-path: /SAKPaaS
    application-display-name: SAKPaaS
spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
#    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: mysecretpassword
